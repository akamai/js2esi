/**
 * unit testing js nested conditionals
 */

if ( X )
  y = 87;


// ensuring hungry if/else matching

if ( A )
  if ( B )
    m0 = 'TT';
  else
    m0 = 'TF';

// forcing a separator (just to clear things out)...
s = 1;

// and the converse

if ( A )
{
  if ( B )
    m1 = 'TT';
}
else
  m1 = 'F?';

